doctype html
html 
    head 
        title= `${title}`
        meta(name="viewport" content="width=device-width, initial-scale=1")
        link(rel="stylesheet", href="/css/style.css")
    body    
        h1 Custom Object Table 
        a(href="/update-cobj") Add/Update this table
        table
            thead
                tr
                    th Name
                    th Strength
                    th Magic
            tbody
                each keyblade in data
                    tr
                        td #{keyblade.properties.name}
                        td #{keyblade.properties.strength}
                        td #{keyblade.properties.magic}
